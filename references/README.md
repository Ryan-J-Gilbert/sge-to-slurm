./techweb/ contains content from BU's TechWeb documentation pages converted to Markdown format.

./man_qsub.txt contains the output from running `man qsub` on the current version of the SCC.

./sbatch.1 contains the official man file for SLURM's sbatch command found at https://github.com/SchedMD/slurm/blob/master/doc/man/man1/sbatch.1